package com.ojas.ra.service.impl;

public class DataInitializer {

	public void initDataBase() {
		// mongoClient.setReadPreference(readPreference);
		/*
		 * MongoDatabase database =
		 * mongoDBClient.getReadMongoClient().getDatabase("spring_security_mongodb");
		 * 
		 * MongoIterable<String> collection = database.listCollectionNames();
		 * MongoCursor<String> iterator = collection.iterator(); while
		 * (iterator.hasNext()) { System.out.println("Database: {}" + iterator.next());
		 * }
		 * 
		 * //mongoDBClient.getReadMongoClient().close(); }
		 */
	}
}